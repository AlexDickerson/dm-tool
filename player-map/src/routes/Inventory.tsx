// Shared party inventory view — live-synced from the DM tool via the
// sidecar service. Placeholder until the sidecar lands.

export function Inventory() {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#999',
        fontSize: 14,
      }}
    >
      Inventory — coming soon.
    </div>
  );
}
