// Aurus leaderboard — ranked list of teams in the race. Live-synced from
// the DM tool via the sidecar service. Placeholder until the sidecar lands.

export function Leaderboard() {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#999',
        fontSize: 14,
      }}
    >
      Aurus leaderboard — coming soon.
    </div>
  );
}
