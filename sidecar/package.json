{
  "name": "dm-tool-sidecar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live-sync service for the DM tool player portal. Holds party inventory and Aurus leaderboard state; broadcasts updates to connected player browsers via WebSocket.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.2",
    "fastify": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
