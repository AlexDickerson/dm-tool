node_modules/
out/
dist/
apps/*/out/
apps/*/dist/
apps/*/server-dist/
packages/*/dist/
*.log
.DS_Store

# Personal config — contains absolute paths to the map-tagger data
config.json

# Environment
.env
.env.*
!.env.example

# Claude Code (worktrees, session data, plans, etc.)
.claude/

# Python artifacts (tagger/)
__pycache__/
*.pyc
*.egg-info/
.venv/
tagger/config.toml
tagger/build/
tagger/dist/
tagger/*.spec

# Auto-Wall binary (downloaded during CI or locally)
auto-wall-bin/

# Runtime state from the player-portal live-sync server (inventory,
# aurus, globe snapshots). Written inside the container via the
# bind-mount volume in docker-compose.yml.
apps/player-portal/data/
